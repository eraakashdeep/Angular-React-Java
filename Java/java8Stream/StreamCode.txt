// 1. Filter Even Numbers

List<Integer> list = Arrays.asList(1,2,3,4,5,6);

list.stream()
.filter(n -> n%2 == 0)
.forEach(System.out::println);


// 2. Find Maximum using stream

int max = list.stream().max(Integer::compare).get();

// 3. Count Elements

long count = list.stream().count();